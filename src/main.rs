mod shared;
mod entry;
mod nikki;
mod subcommand;
mod write;

use subcommand::SubCommand as SubCommand;
use write::Write as Write;
use entry::Entry as Entry;

fn main() {
    println!("Hello, world!");
}
